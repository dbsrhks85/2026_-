-- =============================================
-- departments 테이블에서 아이콘(icon) 컬럼 삭제
-- =============================================

-- 1. 아이콘 컬럼 삭제
ALTER TABLE departments DROP COLUMN IF EXISTS icon;

-- 2. (선택 사항) 기본 부서 데이터에서 아이콘 정보를 제거하고자 할 경우
-- 이미 컬럼이 삭제되었으므로 추가 조치는 필요하지 않으나, 
-- 신규 설치 시에는 아래의 수정된 CREATE TABLE 문을 사용하십시오.
